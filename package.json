{
  "name": "root",
  "private": true,
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "start": "lerna run start --stream --parallel",
    "prebuild": "npm run bootstrap",
    "build": "lerna run build --stream --scope=@todo-list/common"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
